model EmailValidation {
  id         String   @id
  email      String   @unique
  code       String
  expiresAt  DateTime
  isVerified Boolean  @default(false)
  createdAt  DateTime
  updatedAt  DateTime

  @@index([email])
  @@index([createdAt])
  @@map("email_validations")
}
