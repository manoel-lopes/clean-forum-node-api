model User {
  id        String     @id
  name      String
  email     String     @unique
  password  String
  createdAt DateTime
  updatedAt DateTime
  Question  Question[]
  Answer    Answer[]

  @@map("users")
}
